<p style="color: green"><%= notice %></p>

<%= link_to "+ add new patient", new_patient_path %>

<% if @patients.present? %><h4>All Patients</h4><% end %>

<div id="patients">
  <ul>
  <% @patients.each do |patient| %>
    <li><%= link_to patient.name, patient %></li>
  <% end %>
  </ul>
</div>

